# 部署前复制此文件为 .env 并填写实际值
# cp .env.example .env

# JWT 密钥（必填，用于用户认证）
# 生成命令: openssl rand -base64 32
JWT_SECRET=

# 凭证加密密钥（必填，用于加密 Cloudflare API Token）
# 生成命令: openssl rand -base64 24
CREDENTIAL_SECRET=

# GitHub 用户名（用于拉取镜像）
GITHUB_USER=